org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Proxy Configuration
systemProp.http.proxyHost=10.145.253.132
systemProp.http.proxyPort=8080
systemProp.https.proxyHost=10.145.253.132
systemProp.https.proxyPort=8080

# Remove the # below if your proxy requires a username and password
#systemProp.http.proxyUser=YOUR_USERNAME
#systemProp.http.proxyPassword=YOUR_PASSWORD
#systemProp.https.proxyUser=YOUR_USERNAME
#systemProp.https.proxyPassword=YOUR_PASSWORD

# Increase network timeout for slow proxies (in milliseconds, 120000 = 2 minutes)
systemProp.http.connectionTimeout=120000
systemProp.http.socketTimeout=120000
systemProp.https.connectionTimeout=120000
systemProp.https.socketTimeout=120000
